
package VladimirRudenko.app.homework.lesson12;

import java.util.regex.Matcher;
import java.util.regex.Pattern;




/*
public class WhiteCollar extends Human {
     private String company;

     public WhiteCollar(int age, String name, String company){
         super(age, name);
         this.company = company;
     }
    public String getCompany()
    { return company; }

    public void setCompany(String company){
         if (checkCompany(company)) {
             this.company = company;
         } else {
             System.out.println("Company name is invalid");
         }

      public Boolean checkCompany(String company){
        Pattern p = Pattern.compile("^[a-z0-9_-]{3,15}$");
        Matcher m = p.matcher(company);
        return Boolean.TRUE;
        {
            else return Boolean.FALSE; //"Company Name is not valid. Accepted characters: A-z; point, comma, space, dash";
        }

    }
     }
    }
*/


